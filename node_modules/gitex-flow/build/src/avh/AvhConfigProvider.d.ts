import { ConfigProvider } from '../api/ConfigProvider';
import { GitFlowConfig } from '../configs/GitFlowConfig';
/**
 * AVH Configuration provider.
 */
export declare class AvhConfigProvider implements ConfigProvider<GitFlowConfig> {
    private logger;
    private repositoryPath?;
    /**
     * Initializes a new instance of this class.
     *
     * @param repoPath - The path to the git repository.
     */
    constructor(repoPath?: string);
    /**
     * {@inheritdoc}
     *
     * @param config - Git flow config to be set.
     */
    set(config: GitFlowConfig): Promise<void>;
    /**
     * {@inheritdoc}
     *
     * @returns The git flow configuration.
     */
    get(): Promise<GitFlowConfig>;
    private extractConfig;
    private setConfig;
}
